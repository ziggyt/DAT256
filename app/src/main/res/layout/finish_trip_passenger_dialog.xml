<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintlayout"
    android:layout_width="match_parent"
    android:layout_height="200dp">


    <RatingBar
        android:id="@+id/driver_rating_bar"

        style="@android:style/Widget.Material.RatingBar"
        android:layout_width="245dp"
        android:layout_height="49dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="8dp"
        android:isIndicator="false"
        android:numStars="5"
        android:rating="0"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        tools:layout_conversion_wrapHeight="120dp"
        tools:layout_conversion_wrapWidth="500dp" />

    <TextView
        android:id="@+id/rateDriver"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginBottom="8dp"
        android:text="Betygsätt förare"
        app:layout_constraintBottom_toTopOf="@+id/driver_rating_bar"
        app:layout_constraintStart_toStartOf="@+id/driver_rating_bar" />


</androidx.constraintlayout.widget.ConstraintLayout>